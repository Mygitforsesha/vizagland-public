/** Server upload directories — used when mapping stored media records. */
export const PROPERTY_IMAGE_UPLOAD_DIR = '/uploads/property-images/';
export const PROPERTY_DOCUMENT_UPLOAD_DIR = '/uploads/property-documents/';

/** FormData field names for multipart property submission. */
export const MULTIPART_FIELDS = {
  data: 'data',
  propertyImages: 'propertyImages',
  propertyDocuments: 'propertyDocuments',
};
